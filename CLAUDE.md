# טיפול חכם — Mentalytics | הוראות עיצוב ופיתוח

## נהלי עבודה (חובה לכל סשן)

- **סוף כל משימה: `npx tsc --noEmit` ואז commit.** אסור לסיים סשן עם עבודה גמורה לא-מקומטטת או עץ עבודה שלא מתקמפל — סשנים אחרים עובדים על אותו ריפו ויורשים את המצב. אם עבודה נשארה באמצע בכוונה — לציין זאת במפורש בסיכום למשתמש.
- **סשנים מקבילים על אותו ריפו עובדים ב-git worktree מבודד**, במיוחד אם נוגעים באותם קבצים (עמוד האדמין הוא קובץ גדול ורגיש לדריסות).
- **קיים pre-push hook** (`.githooks/pre-push`, מופעל דרך `git config core.hooksPath .githooks`) שמריץ type-check לפני כל push — push שנכשל אומר שיש שגיאת טיפוסים לתקן, לא לעקוף עם `--no-verify`.
- פריסה לאוויר = push ל-master (Vercel פורס אוטומטית). לא דוחפים שינויי תמחור/מוצר בלי אישור מפורש מהמשתמש.

---

## מה האתר הזה

**טיפול חכם (Mentalytics)** הוא פלטפורמה ישראלית להתאמת טיפול נפשי, שנבנתה על ידי פסיכולוג קליני. המערכת משתמשת בשאלונים אדפטיביים כדי לעזור לאנשים למצוא את סוג הטיפול הנכון ואת המטפל המתאים — למבוגרים ולילדים/נוער.

**עיקרון מנחה:** האתר הזה צריך להרגיש כאילו בנה אותו אדם שמבין לעומק כאב אנושי — לא צוות סטארטאפ שמאפטם להמרות.

---

## קהלי יעד (ומצבם הרגשי)

1. **מבוגרים שמחפשים טיפול** — חרדים, כואבים, לא בטוחים. צריכים להרגיש: *"המקום הזה מבין אותי. בטוח להמשיך לקרוא."*

2. **הורים לילדים עם קשיים** — מודאגים, עייפים, לעיתים מוצפים. צריכים להרגיש: *"יש כאן דרך ברורה קדימה."*

3. **אנשי מקצוע בתחום הטיפולי ("בית למפתחים")** — מטפלים, חוקרים, מאבחנים שרוצים לבנות כלים דיגיטליים. **זהו אינקובטור מקצועי — לא עמוד API טכני.**

---

## שפה ומיקום

- **עברית בלבד.** כל הטקסטים בעברית.
- **RTL (ימין לשמאל).** תמיד להשתמש ב-`dir="rtl"` ובמאפייני CSS לוגיים: `margin-inline-start`, `padding-inline-end`, `border-inline-start` וכו'.
- **לא** להשתמש ב-`margin-left/right` או `padding-left/right` — רק מאפיינים לוגיים.

---

## גופנים (חובה)

```css
@import url('https://fonts.googleapis.com/css2?family=Heebo:wght@300;400;500;700;800;900&display=swap');

--font-body:    'Heebo', sans-serif;   /* לכל הטקסטים */
```

- **Heebo בלבד.** עוצב במיוחד לעברית.
- **אסור:** Arial, Inter, system-ui, Rubik כגופן ראשי.

---

## פלטת צבעים

```css
:root {
  /* ירקרק — צבע ראשי */
  --teal:       #3D8C8A;
  --teal-dark:  #2A6462;
  --teal-pale:  #EAF4F3;
  --teal-mid:   #C2DFDE;

  /* זהב — צבע משני */
  --gold:       #D49018;
  --gold-dark:  #A87010;
  --gold-pale:  #FDF6E3;

  /* ורוד — אקסנט */
  --pink:       #F0A8AC;

  /* טקסט */
  --text:       #131F1E;
  --text-2:     #3E5250;
  --muted:      #6B807E;
  --faint:      #A2B5B4;

  /* משטחים */
  --bg:         #FFFFFF;
  --surface:    #F7FAF9;
  --surface-2:  #EFF5F5;
  --line:       #DDE9E8;
}
```

---

## כללי עיצוב

### מה חובה
- רקע ראשי: **לבן** (`#FFFFFF`) — לא ירקרק כבד, לא אפור קר
- צבעים (ירקרק/זהב) מופיעים רק בכפתורים, הדגשות קטנות, ספרות סטטיסטיקה ובלוגו
- `border-radius` על כרטיסים וקונטיינרים: `14px–20px`
- ריווח נדיב בין סקציות: מינימום `80px–100px` padding אנכי
- כפתורים: `border-radius: 50px` (פיל-שייפ תמיד)
- טיפוגרפיה ראשית: `font-weight: 900`, גדולה, אמיצה
- אנימציות כניסה: איטיות ועדינות — `transition: 0.45s ease`

### מה אסור בהחלט
- רקעים כחולים/סגולים או גרדיאנטים קרים
- גריד שלושה עמודות סימטרי עם אייקונים ("מה אנחנו מציעים")
- תמונות פרופיל מלאכותיות או ציטוטים מזויפים
- רקע לבן **קר** (`#FFFFFF` טהור ללא חמימות) — להשתמש ב-`#FFFFFF` עם אלמנטים חמים מסביב
- עיצוב שנראה כמו אפליקציית בריאות גנרית, פינטק, או HR
- חלבובים של אסתטיקה "דב-טקיסטית" ב"בית למפתחים"
- גופן Inter

---

## עיצוב הסקציות

| סקציה | רגש | הנחיה |
|---|---|---|
| Hero | הגעה שקטה, כמו חדר חם | H1 ענק (clamp 3rem–5.5rem), ממורכז, רקע לבן, כפתורי פיל |
| Stats strip | ביטחון, עובדות | רקע `--surface`, מספרים גדולים בירקרק |
| איך זה עובד | ברור, מונחה | 3 צעדים ממוספרים, כרטיסים לבנים עם border |
| למה חשוב | עמקות, חמימות | רקע `--surface`, כרטיסים לבנים |
| שני נתיבים | מבוגרים / ילדים | שתי עמודות — `--teal-pale` ו-`--gold-pale` |
| ציטוט מחקרי | כבדות עריכותית | ממורכז, גופן קל, קו זהב |
| FAQ | ניטרלי, ברור | accordion, רקע `--surface` |

---

## Stack טכני

- **Framework:** Next.js
- **Deployment:** Vercel (`mentalytics.co.il`)
- **סגנון:** להשתמש ב-CSS Modules או Tailwind — לפי מה שקיים בפרויקט
- לא לשנות הגדרות Vercel או domain

---

## קובץ Reference לעיצוב

קובץ ה-HTML הבא הוא ה-reference המלא לעיצוב דף הבית:
`mentalytics-v4.html`

זהו הדגמה ויזואלית מלאה — צבעים, טיפוגרפיה, מבנה סקציות, לוגו SVG, כפתורים, ואנימציות.

---

## לוגו

**מקור האמת ללוגו הוא הקבצים בלבד — אין לצייר או לשחזר אותו מקוד/מתיאור:**
- `public/logo.svg` / `public/logo.png` — הלוגו המלא (טיפול חכם + הסלוגן "מתאימים את החיבור הנכון")
- `public/logo-temp.png` — גרסה רחבה ברזולוציה גבוהה (1000px)
- `public/logo-on-dark-512.png` — גרסה לרקע כהה
- `app/icon.png` / `app/favicon.ico` / `app/apple-icon.png` — סמל favicon: חיתוך ה-ט' וה-ח' המשתלבות מתוך הלוגו האמיתי

מבנה: "טיפול" בירקרק (`#3D8C8A`) ו"חכם" בזהב (`#D49018`) בפונט גיאומטרי ייחודי, כשה-ט' הגדולה (קערה ירקרקה) וה-ח' (קשת זהובה) משתלבות זו בזו. (גרסה קודמת של קובץ זה הכילה snippet SVG של "שתי קשתות" שאינו הלוגו האמיתי — הוסר כדי שלא ישמש בטעות.)
